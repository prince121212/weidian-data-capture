# 微店分类信息爬取使用说明

## 概述

本项目提供了多种方式来爬取微店的分类信息和商品数据。针对您提供的URL `https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all`，我们有以下几种爬取方案：

## 方案一：完整分类爬取（推荐）

使用新创建的 `complete_category_crawler.py` 脚本，这是最全面的解决方案。

### 基本用法

```bash
# 使用默认设置爬取所有分类
python complete_category_crawler.py

# 指定URL和输出文件
python complete_category_crawler.py -u "https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all" -o "data/all_categories.xls"

# 设置请求延迟（避免被封）
python complete_category_crawler.py -d 3.0
```

### 功能特点

- 自动识别店铺的所有分类
- 逐个爬取每个分类下的商品
- 支持API数据提取和页面解析双重模式
- 自动保存为Excel格式
- 包含完整的商品信息（ID、名称、价格、图片、分类等）

## 方案二：基于现有商品数据添加分类

如果您已经有商品列表数据，可以使用现有的分类爬取工具为商品添加分类信息。

### 步骤1：先获取商品列表

```bash
# 爬取所有商品数据
python main.py -u "https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all" -a -o "data/items_all.xls"
```

### 步骤2：为商品添加分类信息

```bash
# 使用分类爬取工具
python category_crawler.py -i "data/items_all.xls" -o "data/items_with_categories.xls" -u "https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all"
```

## 方案三：逐个商品获取分类

使用 `fetch_categories.py` 为每个商品单独获取分类信息。

```bash
python fetch_categories.py
```

## 参数说明

### complete_category_crawler.py 参数

| 参数 | 简写 | 说明 | 默认值 |
|------|------|------|--------|
| --url | -u | 微店店铺链接 | 您提供的URL |
| --output | -o | 输出Excel文件路径 | data/complete_categories_products.xls |
| --delay | -d | 请求间隔时间(秒) | 2.0 |

### category_crawler.py 参数

| 参数 | 简写 | 说明 | 默认值 |
|------|------|------|--------|
| --input | -i | 输入的商品列表Excel文件 | data/items_all.xls |
| --output | -o | 输出的带分类信息Excel文件 | data/items_all_with_categories.xls |
| --url | -u | 微店店铺链接 | 您提供的URL |

## 输出数据格式

爬取的数据将包含以下字段：

- **商品ID**: 商品的唯一标识符
- **商品名称**: 商品标题
- **价格**: 当前售价
- **原价**: 原始价格（如有）
- **销量**: 已售数量
- **库存**: 剩余库存
- **图片链接**: 商品主图URL
- **商品详情页链接**: 商品详情页URL
- **商品分类**: 商品所属分类

## 注意事项

1. **请求频率控制**: 建议设置适当的延迟时间（2-3秒），避免请求过快被网站封禁
2. **网络环境**: 确保网络连接稳定，爬取过程可能需要较长时间
3. **Chrome浏览器**: 脚本会自动下载ChromeDriver，确保系统已安装Chrome浏览器
4. **数据保存**: 程序会定期保存临时数据，防止意外中断导致数据丢失

## 推荐使用流程

1. **首次使用**: 直接运行 `complete_category_crawler.py`，这是最简单和全面的方案
2. **如果遇到问题**: 可以尝试增加延迟时间或使用其他方案
3. **数据验证**: 爬取完成后检查输出的Excel文件，确认数据完整性

## 示例命令

```bash
# 完整爬取（推荐）
python complete_category_crawler.py -u "https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all" -o "data/weidian_all_products.xls" -d 2.5

# 如果上述命令失败，可以尝试分步骤：
# 1. 先爬取商品列表
python main.py -u "https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all" -a -o "data/products.xls" -d 2.0

# 2. 再添加分类信息
python category_crawler.py -i "data/products.xls" -o "data/products_with_categories.xls" -u "https://weidian.com/?userid=1286456178&spider_token=8555&tabType=all"
```

## 故障排除

如果遇到问题，请检查：

1. 网络连接是否正常
2. Chrome浏览器是否已安装
3. Python依赖包是否已安装（运行 `pip install -r requirements.txt`）
4. 目标网站是否可以正常访问
5. 是否需要调整请求延迟时间

## 技术支持

如果在使用过程中遇到问题，可以：

1. 检查控制台输出的错误信息
2. 尝试调整参数（如增加延迟时间）
3. 使用不同的爬取方案
4. 检查网络和浏览器环境
